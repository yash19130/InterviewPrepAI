# Required for real Gemini generation. If unset, the app uses deterministic fallback generation.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Optional later runtime settings.
NEXT_PUBLIC_APP_URL=http://localhost:3000
